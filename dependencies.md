# 📦 Используемые зависимости проекта

Проект на основе **Laravel** с использованием **PostgreSQL** и ряда дополнительных пакетов, обеспечивающих необходимую функциональность.

## Основные зависимости

### **barryvdh/laravel-dompdf**

Генерация PDF-документов из HTML-шаблонов.
Используется для создания договоров, чеков, квитанций, актов и других печатных форм.

---

### **guzzlehttp/guzzle**

HTTP-клиент для интеграции с внешними API.
Применяется для:

* взаимодействия со службами доставки (PickPoint, Почта России),
* отправки данных на Яндекс.Маркет,
* любых REST-запросов к сторонним сервисам.

---

### **laravel/socialite**

OAuth2-авторизация через социальные сети (VK, Google, Facebook и др.).
Используется для входа пользователей без регистрации.

---

### **league/flysystem-aws-s3-v3**

Интеграция Laravel с **AWS S3** — хранение статичных файлов (изображения, документы, отчёты) в облаке.

---

### **league/flysystem-azure-blob-storage**

Аналогичный адаптер для **Microsoft Azure Blob Storage**.
Позволяет гибко переключать провайдер облачного хранилища.

---

### **league/omnipay**

Унифицированный интерфейс для интеграции с различными платёжными шлюзами.
Позволяет принимать онлайн-оплаты (банковские карты, Apple Pay и т.д.).

---

### **maatwebsite/excel**

Генерация и импорт XLS/XLSX-отчётов на основе данных из БД.
Используется для аналитических отчётов, выгрузок для бухгалтерии, статистики продаж и т.д.

---

### **predis/predis**

Клиент для работы с Redis.
Применяется для кеширования сложных SQL-запросов и хранения временных данных в памяти, ускоряя работу приложения.

---

### **spatie/array-to-xml**

Конвертация данных в XML-формат.
Используется для формирования XML-файлов при интеграции с Яндекс.Маркетом.

---

### **spatie/laravel-newsletter**

Интеграция с Mailchimp и другими почтовыми сервисами.
Обеспечивает отправку e-mail уведомлений и рассылок пользователям.

---

### **twilio/sdk**

Интеграция с Twilio для отправки SMS.
Используется для SMS-верификации пользователей при регистрации или авторизации.

---

## Зависимости для разработки и тестирования

### **phpunit/phpunit**

Средство модульного тестирования.
Используется для автоматической проверки бизнес-логики и API.

---

### **mockery/mockery**

Библиотека для создания mock-объектов при тестировании.
Позволяет изолировать тестируемые компоненты.

---

### **fakerphp/faker**

Генерация фейковых данных (пользователи, заказы и т.д.) для тестов и отладки.

---

### **laravel/pint**

Инструмент форматирования кода (code style fixer) по стандартам Laravel.

---

### **nunomaduro/collision**

Улучшает вывод ошибок и трассировку при работе в консоли, особенно во время разработки.

---

### **laravel/sail**

Инструмент для локального запуска Laravel-проекта в Docker.

---

### **laravel/pail**

Реалтайм-логирование событий Laravel в консоли разработчика.

---

## 💾 Хранилище данных

Используется **PostgreSQL** как основная реляционная база данных, а **Redis (через predis)** — для кеширования и очередей.

---

## 🧪 Тестирование

Все тесты выполняются через `phpunit`, с возможностью генерации фиктивных данных с помощью `faker` и подмены зависимостей с `mockery`.

