cd frontend
npm run dev

cd backend
.\venv\Scripts\python manage.py runserver


http://127.0.0.1:5173/

Пробросить порты для подключения к PgAdmin (вместо пароля просто нажать enter)
ssh -p 443 -R0:localhost:8080 free.pinggy.io

Если есть проблема с загрузкой файлов через эндпоинт
sudo chown nginx:nginx /var/lib/nginx/tmp

sudo nft flush ruleset && sudo systemctl restart docker

Подключиться к контейнеру
docker compose exec backend sh

Пересобрать контейнер
docker compose up -d --build backend
