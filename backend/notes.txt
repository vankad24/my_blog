#!/bin/bash
set -e

# Миграции
python manage.py migrate --noinput

# Сборка статики
python manage.py collectstatic --noinput || true

# Gunicorn
gunicorn myblog_backend.wsgi:application \
    --bind 0.0.0.0:8000 \
    --workers 3 \
    --timeout 120